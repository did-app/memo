{
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^6.0.0",
    "@rollup/plugin-replace": "^2.3.3",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.20.0",
    "rollup-plugin-svelte": "^5.0.0",
    "sirv-cli": "^1.0.6",
    "svelte": "^3.0.0"
  },
  "scripts": {
    "serve:dev": "sirv public --dev --single index.html --host 0.0.0.0",
    "build": "rollup -c",
    "build:dev": "rollup -c -w",
    "start:dev": "run-p build:dev serve:dev"
  },
  "dependencies": {
    "dompurify": "^2.0.15",
    "marked": "^1.1.1",
    "page": "^1.11.6",
    "svelte-loading-spinners": "^0.1.1"
  }
}
