{
  "devDependencies": {
    "sirv-cli": "^1.0.6",
    "@rollup/plugin-node-resolve": "^6.0.0",
    "rollup": "^1.20.0",
    "rollup-plugin-svelte": "^5.0.0",
    "svelte": "^3.0.0"
  },
  "scripts": {
    "serve:dev": "sirv --dev --single conversation.html --host 0.0.0.0",
    "build:dev": "rollup -c -w",
    "start:dev": "run-p build:dev serve:dev"
  }
}
